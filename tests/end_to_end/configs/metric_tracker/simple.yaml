name: simple

instance:
  _target_: simplexity.metrics.metric_tracker.MetricTracker
  metric_names:
    cheap:
      - tokens
      - loss
    expensive:
      - lr
      - learning_rate_weighted_tokens
  metric_kwargs:
    ma_window_size: 100
    ema_gamma: 0.9